# Audit Report — 2026-03-21

**Project:** Episodic Blueprint Architecture (EBA)
**Triggered by:** post-harden / on-demand

## Context Accuracy

| File | Status | Issues |
|------|--------|--------|
| project-summary.md | DRIFT (fixed) | Test counts stale (was 24/219, now 27/229), callWithTools providers stale (was Claude-only, now Claude+OpenAI+OpenRouter), model names stale, file counts stale, current state outdated, missing: SOP auto-selection, ai-index fallback, arena real test-runner, ToolShed security model |
| patterns-reference.md | DRIFT (fixed) | callWithTools section said Claude-only (now 3 providers), model tier table wrong, missing: ToolShed security model (validatePath, allowlist), PromptEnhancer callWithTools, SOP auto-selection, AIIndex fallback pattern |
| CLAUDE.md | MISSING | No project-level CLAUDE.md exists |

All drift in project-summary.md and patterns-reference.md has been corrected in this audit.

## Dependency Health
- CVEs: 0
- Outdated: 6 packages (unchanged from prior audit)
  - @anthropic-ai/sdk 0.78.0 to 0.80.0 (minor)
  - openai 6.29.0 to 6.32.0 (minor, in-range)
  - better-sqlite3 11.x to 12.8.0 (major)
  - jest 29.x to 30.3.0 (major)
  - @types/jest 29.x to 30.0.0 (major)
  - @types/node 20.x to 25.5.0 (major)

## Journal Synthesis
- Entries processed: 1
- Learnings extracted: 5 (import patterns, PromptEnhancer forwarding, path containment, command allowlists, execFileSync)
- Risks identified: 4 (better-sqlite3 Win32, GeminiProvider no callWithTools, no CLAUDE.md, uncommitted work)

## Solutions
- Active: 5
- Removed (stale): 0

## Diagrams
- System: regenerated (added: SOP auto-select, sandboxed ToolShed, callWithTools annotations, model-config.json, ai-index fallback)
- Data Flow: regenerated (added: SOP auto-select step, validatePath, model-config.json input)
- Gantt: updated (added hardening section with 5 completed items, remaining section updated)

## Forward Analysis

### Risks
1. **Uncommitted work:** 14 files of security + portability fixes sitting in working tree. Should be committed and pushed.
2. **GeminiProvider lacks callWithTools:** Only provider without tool-calling. Will throw if used as primary in agentic mode.
3. **No CLAUDE.md:** Contributors get no project-specific instructions.
4. **better-sqlite3 Win32:** 10 tests still fail on native Windows. Fallback works but FTS5 quality is reduced.

### Tech Debt
1. **Commit the pending 14-file changeset** — security hardening, callWithTools, SOP auto-select, ai-index fallback.
2. **GeminiProvider callWithTools** — Gemini has native function-calling; implementing it would make all 4 providers tool-capable.
3. **Create CLAUDE.md** — document commands, structure, conventions.

### Opportunities
1. **All critical security issues resolved** — path containment, command injection, bash_execute risk level.
2. **3 of 4 providers now support tool-calling** — system is close to being provider-agnostic for agentic execution.
3. **SOP auto-selection working** — pipeline can now match the right workflow to the task automatically.
4. **Model config externalized** — OpenRouter models no longer hardcoded, can be updated without code changes.

### Dependencies
- @anthropic-ai/sdk 0.80.0 minor bump — check for tool_use API changes before upgrading
- openai 6.32.0 in-range — safe to update via npm install
- better-sqlite3 12.x major — may fix Win32 native binary; evaluate after committing current work
